Choosing the Right Library
Use InMemoryStore if:

You need temporary storage and checkpoints in a single session.
Use FileMemorySaver if:

You want memory to persist across program restarts.
Use DatabaseMemoryStore if:

You're building a scalable application with concurrent memory requirements.
Use RedisMemoryStore if:

You need a distributed system with fast checkpointing.
